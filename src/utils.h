#ifndef UTILS_H_
#define UTILS_H_

/**
 * Call a super advanced AI engine
 */
const int deepThought(const char * question);

/**
 * Return if a given integer number is even
 */
const int isEven(const int);

#endif
